<UserControl x:Class="QRO.Views.SidebarMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QRO.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- Left Navigation Panel -->
        <Grid Grid.Column="0" Width="65">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Background" Value="Red"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                            <Setter Property="Background" Value="Green"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <!-- Navigation Buttons -->
            <StackPanel Grid.Row="0" VerticalAlignment="Top" Margin="5">
                <Button Content="Overlays" Click="OverlayButton_Click" ToolTip="Overlays" Padding="5" Height="50" Background="CadetBlue" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"/>

                <Button Content="Controls presets" Click="ControlsPresetsButton_Click"  Margin="0,5" Padding="5" Height="50" Background="DimGray" Foreground="DarkGray" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"/>

                <Button Content="Graphics presets" Click="GraphicsPresetsButton_Click"  Margin="0,5" Padding="5" Height="50" Background="DimGray" Foreground="DarkGray" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"/>

                <Button Content="Race engineer" Click="RaceEngineerButton_Click" Margin="0,5" Padding="5" Height="50" Background="DimGray" Foreground="DarkGray" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand" />
            </StackPanel>

            <!-- GitHub Button -->
            <Grid Grid.Row="1" Margin="5">
                <Border x:Name="BackgroundBorder" Background="#1a2129" CornerRadius="12"/>
                <Button ToolTip="Visit GitHub Repository" BorderThickness="0" Cursor="Hand" Click="GitHubButton_Click" Height="50">
                    <Image Source="/Resources/Images/GitHub_Dark.png" Margin="6" />
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Border x:Name="BackgroundBorder" Background="#1a2129" CornerRadius="4"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="BackgroundBorder" Property="Background" Value="#3E3E3E"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="BackgroundBorder" Property="Background" Value="#2b2b2b"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="BackgroundBorder" Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
