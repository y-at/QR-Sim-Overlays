<UserControl x:Class="QRO.Views.SidebarMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QRO.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- Left Navigation Panel -->
        <Grid Grid.Column="0" Width="65">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Background" Value="Red"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                            <Setter Property="Background" Value="Green"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>


            <!-- Navigation Buttons -->
            <StackPanel Grid.Row="0" VerticalAlignment="Top" Margin="5">
                <Button Content="Overlays" ToolTip="Overlays" Padding="5" Height="50" Background="CadetBlue" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"/>

                <Button Content="Controls presets" ToolTip="(Coming Soon)" Margin="0,5" Padding="5" Height="50" Background="DimGray" Foreground="DarkGray" BorderThickness="0" IsEnabled="False" Cursor="No"/>

                <Button Content="Graphics presets" ToolTip="(Coming Soon)" Margin="0,5" Padding="5" Height="50" Background="DimGray" Foreground="DarkGray" BorderThickness="0" IsEnabled="False" Cursor="No"/>

                <Button Content="Race engineer" ToolTip="(Coming Soon, basically a chatgpt wrapper fed context to give car setup change suggestions)" Margin="0,5" Padding="5" Height="50" Background="DimGray" Foreground="DarkGray" BorderThickness="0" IsEnabled="False" Cursor="No"/>
            </StackPanel>

            <!-- GitHub Button -->
            <Button Grid.Row="1" ToolTip="Visit GitHub Repository" Margin="5" BorderThickness="0" Cursor="Hand" Click="GitHubButton_Click">
                <!--<Image Source="/Resources/Images/GitHub_Dark.png" /> -->
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#1a2129"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E3E"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</UserControl>
