<UserControl x:Class="QRO.Views.SidebarMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QRO.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid Grid.Column="0" Width="65" Style="{StaticResource ConnectionStatusGridStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Navigation Buttons -->
            <StackPanel Grid.Row="0" VerticalAlignment="Top" Margin="5">
                <!-- Overlays Button -->
                <Button Content="Overlays" Click="OverlayButton_Click" ToolTip="Overlays" Margin="0,0,0,5">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsConnected}" Value="False"/>
                                        <Condition Binding="{Binding SelectedButton}" Value="Overlays"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Background" Value="#8B0000"/>
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsConnected}" Value="True"/>
                                        <Condition Binding="{Binding SelectedButton}" Value="Overlays"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Background" Value="#006400"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Controls Presets Button -->
                <Button Content="Controls presets" Click="ControlsPresetsButton_Click" Margin="0,5">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsConnected}" Value="False"/>
                                        <Condition Binding="{Binding SelectedButton}" Value="ControlsPresets"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Background" Value="#8B0000"/>
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsConnected}" Value="True"/>
                                        <Condition Binding="{Binding SelectedButton}" Value="ControlsPresets"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Background" Value="#006400"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Graphics Presets Button -->
                <Button Content="Graphics presets" Click="GraphicsPresetsButton_Click" Margin="0,5">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsConnected}" Value="False"/>
                                        <Condition Binding="{Binding SelectedButton}" Value="GraphicsPresets"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Background" Value="#8B0000"/>
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsConnected}" Value="True"/>
                                        <Condition Binding="{Binding SelectedButton}" Value="GraphicsPresets"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Background" Value="#006400"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Race Engineer Button -->
                <Button Content="Race engineer" Click="RaceEngineerButton_Click" Margin="0,5">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsConnected}" Value="False"/>
                                        <Condition Binding="{Binding SelectedButton}" Value="RaceEngineer"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Background" Value="#8B0000"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsConnected}" Value="True"/>
                                        <Condition Binding="{Binding SelectedButton}" Value="RaceEngineer"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Background" Value="#006400"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <!-- GitHub Button -->
            <Grid Grid.Row="1" Margin="5">
                <Button ToolTip="Visit GitHub Repository" Click="GitHubButton_Click" Style="{StaticResource NavButtonStyle}" Background="DarkSlateGray">
                    <Image Source="/Resources/Images/GitHub_Dark.png" Margin="6" />
                </Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
